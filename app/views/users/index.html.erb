<h1>Users:<h1>
<%= link_to 'Add User', new_user_path %>
  <table>
    <tr>
      <th>Name</th>
    </tr>

    <% @user.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to 'Show', user_path(user) %></td>
      </tr>
    <% end %>
  </table>
