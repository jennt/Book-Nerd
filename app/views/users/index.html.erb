<h1>Listing Book Nerds<h1>

  <table>
    <tr>
      <th>Name</th>
    </tr>

    <% @user.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to 'Show', user_path(user) %></td>
      </tr>
    <% end %>
  </table>
